# Binaries
/talaria

/dist/
*.exe
*.test

# Go
/vendor/
go.work
go.work.sum

# Coverage
*.out
coverage.*

# Recorded API traffic — may contain real data even after redaction.
# Commit a corpus only after reviewing it; never by accident.
/twin-data/
*.corpus/

# Local config that may hold credentials
.env
.env.*
config.local.yaml

# Ralph loop session logs — raw model output, tens of MB, no archival value
.ralph/*.log
.ralph/review_history
.ralph/state
